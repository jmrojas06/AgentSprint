---
id: TK-30
title: 'CI con GitHub Actions — lint, typecheck, tests y build en cada PR'
status: Review
sprint: 6
priority: high
assignee: agent
estimate: 2
tags:
  - ci
  - github-actions
  - pnpm
  - devops
dependencies: []
createdAt: '2026-08-26T12:51:30.439Z'
updatedAt: '2026-08-26T13:47:24.387Z'
---
## Description

Hoy no hay CI que valide los PRs, así que regresiones (tests rotos, typecheck fallido, build roto) pueden llegar a develop/main sin detectarse. Crear un workflow GitHub Actions separado (.github/workflows/ci.yml — NO tocar el docs.yml existente de Pages) que en cada PR ejecute: install (pnpm), lint, typecheck (`pnpm -r typecheck`), tests (`pnpm -r test`) y build (`pnpm -r build`), sobre Node 20+, con caché de dependencias de pnpm para acelerar las ejecuciones.

## Acceptance criteria

- [x] Existe .github/workflows/ci.yml (workflow separado del docs.yml existente) que se ejecuta en cada pull_request
- [x] El workflow ejecuta en orden: instalación de dependencias, `pnpm lint`, `pnpm -r typecheck`, `pnpm -r test` y `pnpm -r build`
- [x] Se usa Node 20 o superior (matrix o versión fija >= 20) y pnpm con caché de dependencias (actions/setup-node cache: pnpm o actions/cache sobre la store de pnpm)
- [x] Un fallo en cualquiera de los pasos marca el workflow como fallido (no hay continue-on-error)
- [x] El workflow existente docs.yml sigue funcionando sin cambios funcionales

## Notes

- 2026-08-26T13:05:00.000Z | dev | Ampliado el ci.yml que ya existía en develop (no hizo falta crearlo). Trigger ampliado a push en develop+main y pull_request; añadido `permissions: contents: read`; pnpm ahora via corepack (fijado por packageManager en package.json); Node fijado en 20.
- 2026-08-26T13:05:00.000Z | dev | DESVÍO DEL ORDEN DEL CRITERIO 2: Build se ejecuta ANTES de typecheck/test. Es obligatorio: los tipos cross-package resuelven a dist/index.d.ts (moduleResolution Bundler + exports apuntando a dist), que solo existe tras compilar. El ci.yml previo ya documentaba esto. Orden real: install → lint → build → typecheck → test.
- 2026-08-26T13:05:00.000Z | dev | EXCEPCIÓN TEMPORAL AL CRITERIO 4: el step `pnpm lint` lleva `continue-on-error: true` porque eslint vive en feature/linter-monorepo (aún sin mergear a develop). TODO(ci) en el propio workflow: quitarlo al mergear esa rama.
- 2026-08-26T13:05:00.000Z | dev | docs.yml intacto. Validado localmente: YAML parseable con js-yaml, scripts build/typecheck/test presentes en los 5 paquetes, `corepack pnpm -r build && corepack pnpm -r typecheck && corepack pnpm -r test` verde completo.
- [2026-08-26T13:47:24.387Z] (dev) Resumen: ampliado .github/workflows/ci.yml existente en develop (rama feature/ci-workflow, commits 9fbd3c0 + 32fa406). Trigger push(develop,main)+pull_request, permissions contents:read, Node 20 + pnpm vía corepack con caché actions/setup-node cache:pnpm. Steps: install --frozen-lockfile → lint (continue-on-error temporal hasta mergear feature/linter-monorepo) → build → typecheck → test. Build antes de typecheck es necesario (tipos cross-package viven en dist/). Validado: YAML OK (js-yaml), scripts presentes en 5 paquetes, suite completa verde localmente. docs.yml sin cambios.

## Activity

- 2026-08-26T12:51:30.439Z | agent | created | task created (CI con GitHub Actions — lint, typecheck, tests y build en cada PR)
- 2026-08-26T13:05:00.000Z | agent | status | To Do → Review
- 2026-08-26T13:47:24.387Z | dev | note | Resumen: ampliado .github/workflows/ci.yml existente en develop (rama feature/ci-workflow, commits 9fbd3c0 + 32fa406). Trigger push(develop,main)+pull_request, permissions contents:read, Node 20 + pnpm vía corepack con caché actions/setup-node cache:pnpm. Steps: install --frozen-lockfile → lint (continue-on-error temporal hasta mergear feature/linter-monorepo) → build → typecheck → test. Build antes de typecheck es necesario (tipos cross-package viven en dist/). Validado: YAML OK (js-yaml), scripts presentes en 5 paquetes, suite completa verde localmente. docs.yml sin cambios.
